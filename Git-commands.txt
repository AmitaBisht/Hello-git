1. Start git bash and write following commands to push data into github.
commands

1. configure the name and email i.e.
git config --global user.name"username"
git config --global user.email emailaddress.com

2.create a folder or text document.

3.create a new local repository
git init

4. add files 
git add .

5.create new repository in Github
 
6. pass the github clone to git using command
git clone (paste clone)

7.commit
git commit -m "commit message"


8.status
git status

9.To upload repository to Github
git push -u origin master

10.Now check in github

11.status
git status